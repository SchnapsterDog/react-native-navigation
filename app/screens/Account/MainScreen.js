import React from 'react';
import { Text } from 'react-native';

import Screen from '../../components/Screen';

function MainScreen(props) {
  return (
    <Screen>
      <Text>Account / Main Screen</Text>
    </Screen>
  );
}

export default MainScreen;